[tool.ruff]
extend-exclude = [
    "src/clip",
]

[tool.ruff.lint]
select = ["F", "I"]

[tool.mypy]
python_version = "3.11"
mypy_path = "src"
explicit_package_bases = true
exclude = [
    "^src/clip/",
]
ignore_missing_imports = true
