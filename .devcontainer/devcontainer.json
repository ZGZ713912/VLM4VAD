{
  "name": "VLM4VAD",
  "build": {
    "context": "..",
    "dockerfile": "Dockerfile"
  },
  "workspaceFolder": "/workspaces/${localWorkspaceFolderBasename}",
  "remoteUser": "vscode",
  "remoteEnv": {
    "PATH": "/home/vscode/.local/bin:${containerEnv:PATH}"
  },
  "runArgs": [
    "--ipc=host",
    "--shm-size=16g"
  ],
  "containerEnv": {
    "PIP_DISABLE_PIP_VERSION_CHECK": "1",
    "PYTHONDONTWRITEBYTECODE": "1",
    "PYTHONUNBUFFERED": "1",
    "VLM4VAD_TORCH_VARIANT": "cpu",
    "VLM4VAD_TORCH_VERSION": "2.4.1",
    "VLM4VAD_TORCHVISION_VERSION": "0.19.1"
  },
  "mounts": [
    "source=vlm4vad-pip-cache,target=/home/vscode/.cache/pip,type=volume",
    "source=vlm4vad-clip-cache,target=/home/vscode/.cache/clip,type=volume"
  ],
  "customizations": {
    "vscode": {
      "extensions": [
        "ms-python.python",
        "ms-python.vscode-pylance",
        "ms-toolsai.jupyter"
      ],
      "settings": {
        "python.defaultInterpreterPath": "/usr/local/bin/python",
        "python.analysis.extraPaths": [
          "${containerWorkspaceFolder}/src"
        ],
        "terminal.integrated.defaultProfile.linux": "zsh"
      }
    }
  },
  "postCreateCommand": "bash .devcontainer/postCreate.sh",
  "postStartCommand": "mkdir -p outputs"
}
